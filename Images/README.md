# Images

Screenshots for this project live here. **TODO — add the brand logo:**

- `ThisOldScot_Logo.png` — ThisOldScot brand logo (About section).

The README hero uses the existing `PedalManager_OS-home.png`. (Note: this folder is
`Images` with a capital I — GitHub URLs are case-sensitive, so README links use
`Images/`.)
